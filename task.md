### 任务一描述

#### 要求

-[x] 输入可用的主存空间（可变），转变为主存中缓冲区的块
-[x] 输入磁盘空间（可变），转变为磁盘中的块
-[ ] 分别向若干表插入若干记录（给表分配块空间）

#### 实验报告中应体现：

* 设计思路：
  * 简述
  * 流程图

* 测试用例（附截图）
  * 说明输入、输出的数据形式

* 设计过程中遇到了哪些问题，如何解决的。



### 任务2描述：实现DBMS的内存管理

#### 要求：

1. -[ ] 对服务器内存空间进行划分，至少由三部分组成：最近执行过sql的访问计划、数据字典信息、数据处理缓存、日志缓存；

2. -[x] 实现内存空间的管理算法：LRU，CLOCK；

3. 分三种情况实现：
   1. -[x] 访问存储在缓冲池中的页面；
   2. -[x] 从磁盘加载一个页面到空槽；
   3. -[x] 将页面从磁盘加载到牺牲者缓冲池插槽

4. -[x] 内存管理单位与文件块一致。

#### 实验报告中应体现：

* 设计思路：  
  * 简述
  * 流程图
* 测试用例（附截图）
  * 说明输入、输出的数据形式
* 设计过程中遇到了哪些问题，如何解决的。

### 实现B+树的算法：

#### 要求

-[x] 生成
-[x] 插入
-[x] 修改
-[ ] 删除 
-[ ] 针对任务1中的某个具体表，完成create index后，自动生成B+树，并显示索引文件的内容 
-[ ] 定义相关系统表，存放表和索引的定义 
–[ ] 索引也是按照数据页面结构进行存储 
-[x] 根据已知页面大小，计算出每页能存放多少索引项

#### 实验报告中应体现：

* 设计思路：
  * 简述  
  * 流程图  

* 测试用例（附截图）  
  * 说明输入、输出的数据形式  

* 设计过程中遇到了哪些问题，如何解决的。